.git
.gitignore
.venv
venv
__pycache__
*.pyc
.pytest_cache
.ruff_cache
/data
.env
# NOTE: README.md is intentionally NOT ignored — pyproject `readme=` needs it
# in the build context for `pip install .`.
