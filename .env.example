# Copy to .env and adjust.

# Timezone used for timestamps shown in the web UI (IANA name).
TZ=Asia/Seoul

# Where downloads, sqlite db, and cookies live (inside container).
PINCHIVE_DATA_DIR=/data

# Redis connection for the arq task queue.
PINCHIVE_REDIS_URL=redis://redis:6379

# Max concurrent gallery-dl download jobs.
PINCHIVE_MAX_CONCURRENCY=2

# No overall board timeout. Instead, if a single pin download stalls (no data)
# for this many seconds, gallery-dl aborts just that file and continues to the
# next pin. Default 600s (10 min).
PINCHIVE_PIN_STALL_TIMEOUT=600

# Schedules are standard 5-field crontab expressions
# (minute hour day-of-month month day-of-week); an empty value disables the job.
# Also editable at runtime on the Settings page.
#
# Credential keep-alive: re-hit Pinterest and persist the rotated session cookie
# so a registered credential stays alive without re-pasting. Keep it modest —
# hammering Pinterest looks like a bot.
PINCHIVE_REFRESH_CRON=0 */6 * * *

# --- Which image to run ---
# Leave unset to build locally (tagged pinchive:latest), or pull the published
# image:
#   PINCHIVE_IMAGE=ghcr.io/aroxu/pinchive:latest

# gallery-dl sleep between requests (seconds) to stay polite / avoid rate limits.
PINCHIVE_DL_SLEEP=0.8

# Automatic board re-sync: periodically re-download boards to pick up new pins
# (cheap — the per-board archive only fetches new pins). Boards can opt out
# individually in the UI (auto-sync).
PINCHIVE_RESYNC_CRON=30 4 * * *

# Duplicate-image scan schedule.
PINCHIVE_DEDUP_CRON=45 */6 * * *

# UI page sizes.
PINCHIVE_PER_PAGE_BOARDS=24
PINCHIVE_PER_PAGE_PINS=60
PINCHIVE_PER_PAGE_DUPES=20
